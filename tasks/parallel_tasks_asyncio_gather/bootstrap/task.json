{
  "task_name": "parallel_tasks_asyncio_gather",
  "task_description": "# Flyte 2.0 — Parallel Tasks with asyncio.gather\n\n## Background\n\nFlyte 2.0 embraces native Python async primitives for parallelism. Instead of a `@workflow` decorator or `flytekit.map`, you compose parallel execution using `asyncio.gather`. Tasks are `async def` functions decorated with `@env.task` from a `TaskEnvironment`, and you run them with `asyncio.run()`.\n\n## Requirements\n\nCreate the file `/home/user/flyte_project/parallel_pipeline.py` with the following:\n\n### 1. Setup\n\n- Import `asyncio`, `json`, and `flyte`.\n- Create a `TaskEnvironment` named `\"parallel-env\"` (assign it to a variable named `env`).\n\n### 2. Task: `fetch_data`\n\nDefine an async task:\n\n```python\n@env.task\nasync def fetch_data(source: str) -> dict:\n    return {\"source\": source, \"records\": len(source) * 10}\n```\n\n### 3. Task: `aggregate_results`\n\nDefine an async task:\n\n```python\n@env.task\nasync def aggregate_results(results: list) -> dict:\n    return {\n        \"total_sources\": len(results),\n        \"total_records\": sum(r[\"records\"] for r in results),\n        \"sources\": [r[\"source\"] for r in results],\n    }\n```\n\n### 4. Task: `run_parallel_pipeline`\n\nDefine an async task that:\n- Accepts `sources: list`\n- Uses `asyncio.gather(*[fetch_data(s) for s in sources])` to fetch data from all sources **in parallel**\n- Passes the gathered results to `aggregate_results` and returns its output\n\n```python\n@env.task\nasync def run_parallel_pipeline(sources: list) -> dict:\n    results = await asyncio.gather(*[fetch_data(s) for s in sources])\n    return await aggregate_results(list(results))\n```\n\n### 5. `__main__` block\n\nIn the `if __name__ == \"__main__\":` block:\n\n- Call `asyncio.run(run_parallel_pipeline([\"alpha\", \"beta\", \"gamma\"]))`\n- Write the returned dict as JSON to `/home/user/flyte_project/pipeline_result.json`\n\n```python\nif __name__ == \"__main__\":\n    result = asyncio.run(run_parallel_pipeline([\"alpha\", \"beta\", \"gamma\"]))\n    with open(\"/home/user/flyte_project/pipeline_result.json\", \"w\") as f:\n        json.dump(result, f)\n```\n\n## Implementation Guide\n\n1. Do **not** use `@workflow` or `flytekit.map` — they do not exist in Flyte 2.0.\n2. Parallelism is achieved via `asyncio.gather`, which runs multiple async task coroutines concurrently.\n3. All task functions must be `async def` and decorated with `@env.task`.\n4. Running the script with `python3 /home/user/flyte_project/parallel_pipeline.py` must:\n   - Execute without errors\n   - Produce `/home/user/flyte_project/pipeline_result.json`\n\n## Constraints\n\n- **Project path**: `/home/user/flyte_project`\n- **Script**: `/home/user/flyte_project/parallel_pipeline.py`\n- **Result file**: `/home/user/flyte_project/pipeline_result.json`\n- The `flyte` package is already installed — do **not** reinstall it.\n- Use only the standard library plus `flyte`.\n",
  "truth": "## Verification Plan\n\n### 1. File existence\n- `/home/user/flyte_project/parallel_pipeline.py` exists.\n\n### 2. Script runs successfully\n- Running `python3 /home/user/flyte_project/parallel_pipeline.py` exits with returncode 0.\n\n### 3. Result file produced\n- `/home/user/flyte_project/pipeline_result.json` exists and contains valid JSON.\n\n### 4. Result correctness\n- `total_sources` == 3\n- `total_records` == 140 (\"alpha\"=5 chars×10=50, \"beta\"=4 chars×10=40, \"gamma\"=5 chars×10=50)\n- `sources` list contains exactly \"alpha\", \"beta\", and \"gamma\"\n\n### 5. Parallel pattern used\n- The source file contains `asyncio.gather` (syntactic check — no CLI can verify the parallel pattern at the Python-AST level).\n"
}
